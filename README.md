
![Project Illustration](https://v3b.fal.media/files/b/0a9b9c64/6tbp6g6zdvsJPuEz3FkY2_1iCCEv35.png)

# open-finance-br-cli

CLI para Open Finance Brasil — conectividade bancária padronizada

## 🤖 Onboarding para Agentes

Este projeto foi mapeado com [llm-project-mapper](https://github.com/wesleysimplicio/llm-project-mapper) e possui:

- `AGENTS.md` — Documentação para agentes de IA
- `CLAUDE.md` — Regras para Claude Code
- `INIT.md` — Bootstrap para agentes
- `.agents/` — Configurações de agentes
- `.skills/` — Skills locais (se houver)

**Status:** ✅ `ralph_ready: true` — Pronto para loops de goal do Hermes Turbo.

## Instalação

```bash
pip install -e .
```

## Uso

```bash
open-finance-br --help
```

## Contribuição

Projeto parte da iniciativa de **Contribuição OSS Brasil** — ferramentas financeiras para o ecossistema nacional.

---
*Generated by Hermes Turbo*



## 🔌 API & Endpoints

**API Documentada:** Open Finance Brasil (OFB)

**Quantidade de Endpoints:** Múltiplos (bancos participantes)

**Base URL:** `https://api.openfinance.br (ou sandbox)`

**Documentação Oficial:** https://openfinancebrasil.org.br/

**Cobertura de Endpoints:**
Consentimentos, recursos, dados de usuários, bancos, investimentos

**Autenticação:**
- OAuth 2.0 / Client Credentials (padrão Open Finance / bancos)
- Tokens gerenciados automaticamente pelo CLI
- Configuração via variáveis de ambiente ou arquivo de config

**Exemplo de uso:**
```bash
# Listar endpoints disponíveis
open-finance-br --help

# Exemplo de chamada (ajustar conforme CLI)
# open-finance-br account list
```
---

# open-finance-br-cli

CLI Python para consumir APIs padronizadas do Open Finance Brasil como
consumidor. Ele usa apenas biblioteca padrao (`urllib`, `argparse`, `json`) e
gera respostas JSON coloridas no terminal.

## Specs usadas

- Accounts v2.4.2: https://raw.githubusercontent.com/OpenBanking-Brasil/openapi/main/swagger-apis/accounts/2.4.2.yml
- Consents v3.0.1: https://raw.githubusercontent.com/OpenBanking-Brasil/openapi/main/swagger-apis/consents/3.0.1.yml
- Resources v3.0.0: https://raw.githubusercontent.com/OpenBanking-Brasil/openapi/main/swagger-apis/resources/3.0.0.yml
- Participantes do diretorio: https://data.directory.openbankingbrasil.org.br/participants

Os mocks retornam payloads com os campos obrigatorios dessas specs para permitir
desenvolvimento local quando nao ha instituicao real configurada.

## Instalacao

```bash
python3 -m pip install -e .
openfinance --version
```

O comando instalado no PATH e `openfinance`.

## Configuracao

```bash
openfinance config set \
  --base-url https://api.banco.com.br/open-banking/accounts/v2 \
  --token-url https://auth.banco.com.br/oauth2/token \
  --client-id "$CLIENT_ID" \
  --client-secret "$CLIENT_SECRET" \
  --certificate /path/cert.pem \
  --certificate-key /path/key.pem
```

URLs por familia podem ser configuradas separadamente quando a instituicao
publicar bases diferentes:

```bash
openfinance config set \
  --accounts-url https://api.banco.com.br/open-banking/accounts/v2 \
  --consents-url https://api.banco.com.br/open-banking/consents/v3 \
  --resources-url https://api.banco.com.br/open-banking/resources/v3
```

Arquivos locais:

```bash
openfinance config path
openfinance config show
```

Por padrao os arquivos ficam em `~/.openfinance/config.json` e
`~/.openfinance/token_cache.json`. Para testes, use `OPENFINANCE_CONFIG` e
`OPENFINANCE_TOKEN_CACHE`.

## Token OAuth2

```bash
openfinance token --scope "openid accounts consents resources"
openfinance token --force
```

Quando nenhuma instituicao real ou credencial OAuth esta configurada, o CLI
retorna e cacheia um token mock. Em uso real, configure `token_url`, `client_id`
e `client_secret`; o token e obtido por OAuth2 `client_credentials` com HTTP
Basic Auth e corpo `grant_type=client_credentials`.

## Contas

```bash
openfinance accounts
openfinance accounts --page 1 --page-size 25 --account-type CONTA_DEPOSITO_A_VISTA
openfinance account mock-account-001
openfinance balances mock-account-001
openfinance transactions mock-account-001 --from-booking-date 2026-01-01 --to-booking-date 2026-05-25
openfinance transactions-current mock-account-001
```

## Consentimentos

Criar consentimento com payload montado pelo CLI:

```bash
openfinance consents create \
  --cpf 12345678901 \
  --permissions ACCOUNTS_READ ACCOUNTS_BALANCES_READ ACCOUNTS_TRANSACTIONS_READ RESOURCES_READ \
  --expiration-date-time 2026-12-31T23:59:59Z
```

Usar payload JSON proprio:

```bash
openfinance consents create --payload consent.json
openfinance consents get urn:openfinancebrcli:example
openfinance consents delete urn:openfinancebrcli:example
```

## Diretorio e recursos

```bash
openfinance participants --limit 5
openfinance participants --sandbox --limit 5
openfinance resources --page 1 --page-size 25
```

`participants` consulta o endpoint publico do diretorio central, salvo quando
`--mock`, `--mock-response` ou `config set --mock true` forem usados.

## Mock

Sem `base_url`, os comandos de instituicao (`accounts`, `account`, `balances`,
`transactions`, `transactions-current`, `consents`, `resources`) retornam mocks
schema-shaped. Tambem e possivel forcar:

```bash
openfinance --mock accounts
openfinance config set --mock true
```

## Validacao local

```bash
python3 -m unittest discover -s tests
python3 -m compileall openfinance tests
python3 -m openfinance --no-color accounts
```

## O que já foi feito aqui

Este repositório já passou pela primeira camada de estruturação para agentes e engenharia:

- mapeamento com `llm-project-mapper`
- arquivos de contexto para agentes: `AGENTS.md`, `CLAUDE.md`, `INIT.md`
- estrutura de apoio com `.agents/`, `.skills/` e `.specs/`
- README reorganizado para leitura humana e por agentes
- documentação orientada a onboarding, API e uso prático
- imagem temática no topo para identificação rápida do projeto
- repositório público sincronizado no GitHub

## Automação vs equipe humana

A comparação abaixo é uma **estimativa conservadora** para a fase de descoberta, documentação e padronização inicial.

| Abordagem | Pessoas típicas | Tempo de setup inicial | Observações |
|---|---:|---:|---|
| Time manual | PM + engenheiro(a) + QA + DevOps + técnico(a) de documentação | 2 a 5 dias úteis | exige alinhamento, reuniões e revisão cruzada |
| Fluxo automatizado | 1 engenheiro(a) orquestrando agentes | 1 a 3 horas | reaproveita contexto, reduz retrabalho e padroniza saída |

**Economia estimada:** entre **70% e 90%** do tempo de setup e documentação inicial, dependendo do estado original do projeto.

## Para engenheiros e mantenedores

Se você vai continuar evoluindo este projeto:

1. Leia `AGENTS.md` antes de editar qualquer coisa.
2. Use a skill do projeto para entender o vocabulário e os fluxos esperados.
3. Mantenha o README, a skill e a implementação sincronizados.
4. Prefira mudanças pequenas, auditáveis e fáceis de revisar.
5. Não exponha credenciais, tokens ou dados de sessão.
6. Quando fizer uma mudança relevante, atualize também a documentação de API e os exemplos de uso.
